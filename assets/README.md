README assets for RealRestorer live here.

Current files:

- `teaser.svg`: default teaser figure used by the root `README.md`

You can replace `teaser.svg` with the final project overview figure later without touching the README layout.
