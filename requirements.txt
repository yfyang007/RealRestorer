# Important:
# Do NOT install upstream `diffusers` from PyPI for this project.
# You must install the patched RealRestorer-enabled `diffusers` repo separately.
# The versions below are pinned from the `kontext` conda environment.

torch==2.5.1
torchvision==0.20.1
transformers==4.57.3
accelerate==1.10.1
safetensors==0.5.3
Pillow==11.3.0
numpy==2.0.1
opencv-python==4.12.0.88
scipy==1.13.1
scikit-image==0.24.0
omegaconf==2.3.0
timm==1.0.20
einops==0.8.2
huggingface_hub==0.36.2
